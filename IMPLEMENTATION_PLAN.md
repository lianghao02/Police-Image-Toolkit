# 實作計畫

## 目標與驗收條件

- 維持既有三個分頁入口與核心影像處理流程，不新增儀表板或重寫演算法。
- 圖片轉檔、影片逐格截圖與長截圖分頁的輸出資料夾皆建立或更新 `report_index.json`，可由輸出檔名回溯來源檔名、工具類型、影片時間與輸出像素尺寸。
- 索引只保存檔名與必要技術資訊，不保存絕對路徑、案號、姓名或其他使用者未輸入的資料。
- 來源檔與既有輸出檔名防碰撞機制維持不變；索引更新失敗不可影響已成功輸出的影像。
- 既有核心服務測試、Release 建置與 QA 通過。

## 不做範圍

- 不更換 C# 12 / .NET 8 / WPF 與既有影像、影片處理方式。
- 不修改 Photo-Report-Generator、不中斷其既有專案檔格式，也不做跨 Repository 自動匯入。
- 不重做工具入口、長圖切分演算法、影片逐格截圖核心或全面更換命名規則。
- 不加入案件管理、雲端上傳、AI 辨識、背景遙測或新的工具分頁。

## 已確認決策

- 目前首頁已使用三個清楚的工具分頁，未發現「所有控制項同時堆疊」問題，保持原設計。
- `ImageService`、`VideoService` 與 `LongScreenshotService` 均以新檔方式寫入，且使用 `FileMode.CreateNew` 避免同名覆寫；本輪不改動這條證物安全邊界。
- 影片與長圖輸出使用 `001` 流水號後，缺少程式關閉後仍可保留的來源／時間對照；以資料夾內中繼索引補足。
- Photo-Report-Generator 目前只讀取其 ZIP 專案內的 `manifest.json`，未讀取通用 `report_index.json`；本輪僅產生低耦合索引並提出下游讀取建議。

## 工作清單

- [x] 建立可原子更新的輸出索引服務與核心服務測試。
- [x] 圖片轉檔完成後，依輸出資料夾寫入來源與輸出對照。
- [x] 影片截圖新增／刪除時同步更新索引，並在狀態列說明結果；「開啟截圖資料夾」會前往實際 `<影片名>_Snapshots` 資料夾。
- [x] 長截圖分頁匯出後寫入頁面、來源與像素尺寸對照。
- [x] 更新 README、MEMORY 與 HANDOFF 的實際命名及中繼索引說明。
- [x] 執行核心測試、QA、Git diff 檢查與 Release 建置。

## 風險與因應

- 使用者若以其他程式鎖住 `report_index.json`，影像輸出仍保留，介面會提示索引未更新；不刪除或覆寫既有影像。
- 下游工具尚未讀取索引：維持 JSON schema 小而固定，待 Photo-Report-Generator 另案採用，避免本輪跨 Repository 耦合。

## 驗證紀錄

- `scripts\test.ps1`：8/8 項核心服務測試通過，包含 PNG↔JPG 轉檔後的來源 SHA-256 不變、影片流水號、長圖分頁與索引新增／刪除。
- `scripts\qa.ps1`：Release 建置 0 警告、0 錯誤；核心服務測試 8/8 通過。
- `scripts\build.ps1`：成功建立 `dist\PoliceImageToolkit.exe`；SHA-256 與 `dist\SHA256SUMS.txt` 相符。
- **發布結案判定**：本實作計畫工作項目已 100% 交付，v11.4.0 正式完成驗證並收斂結案，專案進入 Stable / Maintenance。
