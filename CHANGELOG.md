# 變更紀錄 (CHANGELOG)

## 🚀 v11.4.0 (2026-09-09)

- **輸出追溯索引 (`report_index.json`)**：
  - **三大工具統一支援**：圖片批次轉檔、影片逐格截圖與長截圖分頁輔助皆會在輸出資料夾建立或原子更新 `report_index.json`。
  - **來源與規格完整追溯**：
    - 圖片轉檔：保存輸出檔名、來源檔名、工具類型與像素尺寸。
    - 影片截圖：保存輸出檔名、來源影片檔名、工具類型、截圖在影片中的時間點與像素尺寸。
    - 長截圖分頁：保存分頁輸出檔名、原始長圖檔名、工具類型、頁面序號與分頁像素尺寸。
  - **同步刪除與復原**：影片逐格截圖執行 `Z`（復原上一張）或刪除截圖時，自動同步移除 `report_index.json` 內對應項目。
  - **安全與隱私防護**：索引採原子寫入防毀損；僅保存檔名與技術中繼資訊，不儲存絕對路徑、使用者電腦路徑、案號或姓名等額外個資。
  - **自訂輸出路徑體驗**：修復自訂輸出目錄時，「開啟截圖資料夾」精準前往實際 `<影片名>_Snapshots` 目錄。
- **現代專業柔和淺色調 UI/UX 升級 (Light Morandi & Commercial UX)**：
  - **WPF 中文字清晰度重構**：全面啟用 `TextFormattingMode="Display"`、`TextRenderingMode="ClearType"`、`UseLayoutRounding="True"` 與 `SnapsToDevicePixels="True"`，徹底根治中文字筆畫模糊發虛問題。
  - **莫蘭迪冷灰與霧藍色系**：切換為低眩光淺色護眼調性（`#F1F4F8` / `#E5EBF2` 底色、純白卡片、霧藍 `#3E5E7A` 與鼠尾草綠 `#347555` 動作按鈕）。
  - **表單元件標準化**：輸入框高度統一為 32px、圓角 5px、Padding `8,5`，具備聚焦外框；按鈕支援點擊微位移手感；頂部導航升級為 Segmented Control 膠囊底槽與浮動卡片。
  - **工作台視覺重構**：圖片轉檔提供大面積虛線拖曳熱區與四大手機格式膠囊 Badge；影片逐格截圖採用深色手機邊框工作台降低畫面干擾；長截圖分頁集中圖框規格設定與公務標準提示。
- **維護與專案純化**：
  - 移除過期且未被引用的舊版發布手冊備忘檔案。

## 🔮 未來展望 (Roadmap)
- **Photo-Report-Generator 讀取索引**：由下游工具讀取既有 `report_index.json`，協助帶入圖片與來源資訊；此功能需於該 Repository 另案實作。
- **滑鼠滾輪逐格步進 (Mouse Wheel Scrubbing)**：手機預覽螢幕支援滑鼠滾輪向上/向下微調 0.1 秒，達成「右手滾輪定位、左手 Space 秒截」極速盲操。
- **多倍速播放切換 (1.0x / 1.5x / 2.0x)**：長影片快速瀏覽跳轉涉案段落。
- **長截圖分頁切線微調 (±20px)**：手動拖曳避開切割貼圖與關鍵轉帳單據本體。

## 🚀 v11.3.0 (2026-08-31)
- **版本宣告與徽章體系**：
  - 新增 `src/PoliceImageToolkit/version.txt` 宣告檔，建置與發布時同步輸出至 `dist/version.txt`。
  - 主介面右上角動態載入版本徽章膠囊標籤，清楚標示當前安裝版本。
- **GitHub Release 自動檢查更新**：
  - 新增 `IUpdateService` 與 `UpdateService`，點擊『🔄 檢查更新』非同步向 GitHub Release API 查詢最新發行資訊。
  - **最新版提示**：若已是最新版，跳出資訊框確認目前版本狀態。
  - **發現新版提示**：展示新版版號與更新重點摘要，確認後直接導引至 GitHub 下載頁面或直接取得單檔。
  - **離線與網路防護**：公務封閉內網或連線逾時（6 秒）時顯示友善提醒，防範程式崩潰或未預期例外。
- **專案結構純化**：
  - 徹底移除舊版純前端封存（`legacy_web/`）與根目錄冗餘檔案，確認為純粹的 C# WPF 單一專案結構。
  - 補強 `.gitignore` 支援深層 `**/bin/`、`**/obj/`、`crash.log` 等快取過濾。

## 🚀 v11.2.0 (2026-08-26)
- **長截圖分頁輔助（第三個分頁）**：
  - 新增 `LongScreenshotSplitView` 與 `LongScreenshotSplitViewModel`，支援將超長手機對話截圖依 Word/Photo-Report-Generator 圖框比例（預設 `8 cm × 17.5 cm`、重疊 `5 mm`）切分為連續衍生圖片。
  - **原始像素裁切**：堅持原始點陣裁切，不縮放、不重取樣、不在圖片上烙印遮罩或頁碼，保持原始字體清晰度。
  - **前後頁重疊遮罩防漏字**：相鄰頁保留固定重疊像素，黃色斜線遮罩僅供手機框預覽比對，不輸出至圖檔。
  - **跨執行緒點陣圖修復**：修正背景 `Task.Run` 裁切時的 WPF 跨執行緒存取例外，採用 `BitmapCacheOption.OnLoad` + `.Freeze()` 確保執行緒安全。
  - **低解析度常駐警示**：來源圖寬度低於 `600px` 時自動警示放進 Word 可能模糊，引導使用者索取無失真原始檔案。
- **介面與工作區優化**：
  - 影片逐格截圖頁調整為左右等分大佈局，手機框採等比例放大，右側配置對照圖與證物清單。
  - 圖片批次轉檔頁整合「開啟輸出資料夾」與「變更輸出路徑」於同一功能分區。
  - 統一中文介面字型堆疊與文字層級排版。
- **架構與防禦性強化**：
  - 修正影像與影片輸出處理的原子寫入與暫存清理防禦機制。
  - 專案記憶庫 `MEMORY.md` 與 `IMPLEMENTATION_PLAN.md` 完整同步。

## 🚀 v11.1.0 (2026-08-25)
- **專屬警務鑑識圖示**：全新設計包含手機、鏡頭快門光圈與警徽盾牌之多解析度圖示（`app.ico`、`app_icon.png`），全面替換應用程式與視窗標題列 Logo。
- **手機工作台大視窗優化**：
  - 手機外框放大自適應工作台（`寬 360px × 高 580px`），影片完整貼合內螢幕，去除重疊的假動態島，文字清晰易讀。
  - 移除多餘遮罩條帶與遮罩裁切功能，介面乾淨純粹。
  - 預設取消畫面內部深色時間戳條帶，輸出 100% 純淨原始影像；檔名完整保留毫秒時間戳與案號標籤。
- **上頁末行對照視窗**：即時預覽前一張截圖與毫秒時間戳，滾動比對防漏截。
- **全域鍵盤快捷鍵系統**：
  - 支援 `Space` 秒截、`←`/`→` 0.1s 步進、`↑`/`↓` 1.0s 跳轉、`Z` 復原上一張、`P`/`Enter` 播放暫停。
  - 文字輸入時自動旁路，操作不衝突。
- **建置腳本升級**：
  - `build.ps1` 支援發布前進程關閉、快取清理、移除 pdb 檔案，並自動觸發 Windows Shell 圖示更新。
- **文件體系**：新增 `MEMORY.md` 專案記憶庫，更新 `AGENTS.md` 與 `README.md`。

## 🚀 v11.0.0 (2026-08-24)
- **重大架構重構**：全面升級為 Windows 原生 **C# (.NET 8 LTS / WPF)** 桌面應用程式，原純前端網頁版本移至 `legacy_web/` 歷史隔離封存。
- **單檔免安裝發布**：支援 Self-Contained Single-File 發布，產出單一免安裝執行檔（`PoliceImageToolkit.exe`），無須預裝 .NET Runtime，點擊即開。
- **手機圖片批次轉檔引擎**：
  - 多核心並行管線，原生支援 iPhone HEIC/HEIF、WebP、PNG、JPG、BMP、TIFF 格式互轉。
  - 支援 Exif 方向自動校正與等比例縮放控制。
- **手機影片逐格截圖引擎**：
  - 原生硬體加速播放，解決 Windows 缺乏 HEVC 延伸模組導致 iPhone 4K 60fps MOV 黑畫面問題。
  - 支援毫秒級（~33ms）上一格／下一格精準步進。
- **自動化維護**：新增 `scripts/build.ps1` 一鍵發布腳本與 `scripts/qa.ps1` 自動化建置檢核。

## 🏆 v10.0.2 (2026-08-17)
- **使用指南**：補齊下載、瀏覽器需求、功能入口與靜態部署方式。
- **依賴揭露**：明列 CDN 第三方函式庫與完全離線部署條件。
- **敘述校正**：移除未經量測的速度與絕對資安宣稱，保留可驗證的本機處理邊界。

## 🏆 v10.0.1 (2026-08-13)
- **影片穩定性**：補強載入逾時、解碼失敗、seek 無回應與快照例外處理，確保操作鎖可恢復。
- **資源釋放**：圖片 Object URL 與暫用 Canvas 在成功或失敗後均釋放。
- **PDF 頁碼**：反向範圍會自動正規化，並限制在實際頁數內。
- **公文影像保護**：橫紋濾除新增紅印與連續文字筆畫保護條件。
- **文件**：README 移除不可驗證效能宣稱，補充證物保存與瀏覽器相容性限制。

## 🚀 v10.0 (2026-08-12)
- **核心功能**：全面整合 HEIC 免費本機轉檔、PDF 頁面解析與司法鑑識對話截圖工作室。
- **影像強化濾鏡**：新增 Unsharp Mask 銳化演算法與拉普拉斯 (Laplacian) 二值化對比度調校，支援拋出車牌號碼與監視器人臉細節。
- **公文淨化**：優化 PDF 掃描件橫向細紋與條紋殘影濾除演算法 (`removeScanlineStreaks`)。
- **架構相容**：修復檔案編碼為標準 UTF-8 (無 BOM)，維護 100% 前端瀏覽器離線安全防禦機制。

## 📦 v1.1.0 (2026-08-10)
- **核心架構更新**：警務影像處理模組與快取機制優化。
- **效能優化**：設置本機快取目錄，提昇轉檔與快取處理效率。
